# Radidone Web App

Minimal React dashboard for testing the core Radidone flows independently:

- X-ray upload and preview
- Session start and phase stepping
- Voice input and voice output testing

## Run locally

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if you want to point the dashboard at a backend API.
